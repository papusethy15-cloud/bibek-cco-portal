# Production environment — used by Vite during `npm run build` on the VPS.
# .env.local overrides this during local development (and is gitignored).
VITE_API_URL=https://api.bibekenterprises.com/api/v1
